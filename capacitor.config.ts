import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.mbtopografia.controleequipamentos',
  appName: 'M&B Controle de Equipamentos',
  webDir: 'dist',
  android: {
    backgroundColor: '#020617',
  },
};

export default config;
